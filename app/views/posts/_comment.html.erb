<tr>
    <td><%= comment.content %></td>
    <td><%= comment.user.email %></td>
    <td><%= link_to 'Destroy', post_comment_path(post_id, comment), method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
